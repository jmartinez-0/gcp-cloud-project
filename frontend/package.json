{
  "scripts": {
    "generate-config": "node ../scripts/generate-config.mjs",
    "start": "npx http-server . -p 8080"
  },
  "dependencies": {
    "firebase": "^12.18.0",
    "http-server": "^14.1.1"
  }
}
